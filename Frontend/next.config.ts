import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['www.it.kmitl.ac.th', 'test-api-66070006.s3.amazonaws.com'] // Add the hostname here
  },
};

export default nextConfig;
